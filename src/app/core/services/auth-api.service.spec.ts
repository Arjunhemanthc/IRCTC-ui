import { describe, it, expect } from 'vitest';

describe('AuthApiService', () => {
  it('should be created', () => {
    expect(true).toBeTruthy();
  });

  it('should call login endpoint with correct URL and payload', () => {
    expect(true).toBeTruthy();
  });
});
